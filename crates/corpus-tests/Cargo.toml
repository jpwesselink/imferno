[package]
name = "corpus-tests"
version = "0.1.0"
edition = "2021"
description = "Corpus-based regression tests for the imferno SMPTE IMF validator"
publish = false

[lib]

[dependencies]
imferno-core = { path = "../imferno-core" }
